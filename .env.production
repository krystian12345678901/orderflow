# OrderFlow Production Environment Variables
# Skopiuj do backend/.env i ZMIEŃ wartości!

NODE_ENV=production
PORT=5000

# WAŻNE: Zmień te sekrety na losowe ciągi znaków (min 32 znaki)!
# Wygeneruj np. przez: openssl rand -base64 32
JWT_ACCESS_SECRET=WYGENERUJ_TUTAJ_LOSOWY_CIĄG_MIN_32_ZNAKI
JWT_REFRESH_SECRET=WYGENERUJ_TUTAJ_INNY_LOSOWY_CIĄG_MIN_32_ZNAKI

# W produkcji z proxy (nginx) nie musisz ustawiać FRONTEND_URL
# FRONTEND_URL nie jest używany gdy NODE_ENV=production
